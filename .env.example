# Database (Neon Postgres)
DATABASE_URL="postgres://user:password@host/db?sslmode=require"

# Admin
# Generate bcrypt hash: node -e "console.log(require('bcryptjs').hashSync('yourpass', 10))"
ADMIN_PASSWORD_HASH=""
# Generate: openssl rand -base64 32
ADMIN_JWT_SECRET=""

# Credential encryption
# Generate: openssl rand -base64 32
STOCK_ENCRYPTION_KEY=""

# Email (Resend)
RESEND_API_KEY=""
RESEND_FROM="Claude Shop <no-reply@example.com>"

# Bank (Sepay QR) — BANK_BIN = mã ngân hàng viết tắt (vd "ACB", "VCB", "MB")
BANK_BIN="ACB"
BANK_ACCOUNT_NO=""
BANK_ACCOUNT_NAME=""

# Site
NEXT_PUBLIC_SITE_URL="http://localhost:3000"
NEXT_PUBLIC_SITE_NAME="Claude Shop"
