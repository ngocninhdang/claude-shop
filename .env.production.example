# Production env — copy sang .env.production trên VPS, KHÔNG commit file thật

# Neon Postgres (pooler endpoint, ?sslmode=require)
DATABASE_URL=""

# Admin — sinh lại hash khác admin123 cho prod!
# node -e "console.log(require('bcryptjs').hashSync('YOUR_PROD_PASSWORD',10))"
# Nhớ escape dollar sign: thay $ thành \$
ADMIN_PASSWORD_HASH=""

# openssl rand -base64 32
ADMIN_JWT_SECRET=""

# openssl rand -base64 32
STOCK_ENCRYPTION_KEY=""

# Resend
RESEND_API_KEY=""
RESEND_FROM="Claude Shop <no-reply@nguyentheduc.com>"

# Bank (Sepay QR) — BANK_BIN = mã/tên viết tắt ngân hàng (vd "ACB", "VCB", "MB")
BANK_BIN="ACB"
BANK_ACCOUNT_NO="8352771"
BANK_ACCOUNT_NAME=""

# Site
NEXT_PUBLIC_SITE_URL="https://claude.nguyentheduc.com"
NEXT_PUBLIC_SITE_NAME="Claude Shop"
